import styled from "styled-components";

const Box = styled.section`
  display: flex;

  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;

  gap: 1em;
  margin: 2em;
`;

export default Box;
